# Runtime 설정

Reference Runtime과 이후 QRB2210 backend의 실행 설정을 둘 폴더다.

예정 설정:

- `reference.json`: 검증할 Tensor ID, dump 여부, 허용 오차 정책
- `qrb2210.json`: alignment, thread 수, 선택 backend와 kernel 옵션

설정 파일이 실제 코드에서 사용되기 전에는 설명과 schema를 먼저 확정한다.
