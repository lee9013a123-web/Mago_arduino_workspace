# 작업 인계 — 2026-08-19

## 1. 한 줄 요약

**v2 최종 suite가 RTF 0.3457을 달성**했다(v1 0.5223 대비 1.51배, 최초 baseline 7.0019 대비 20.25배).
v5(신규 QConv MAC + address 계층)는 **빌드 중단 상태**이며, 내일 재빌드부터 시작하면 된다.

---

## 2. 지금 당장 할 일

빌드가 12개 타깃 중 7개에서 끊겼다(09:07). 증분 컴파일이 없어 **처음부터 10~11분** 걸린다.

```bash
cd /home/arduino/workspace/egs/accelerate_CAM
bash scripts/4_profill/optimization/qconv_v5/01_build_qconv_v5.sh
```

빌드 후:

```bash
./build/profill/optimization/test_qconv_v5_primitives
./build/profill/optimization/test_qconv_v5_address
./build/profill/optimization/test_qconv_candidate

python3 scripts/4_profill/optimization/qconv_v5/03_benchmark_qconv_v5.py --preflight-only
python3 scripts/4_profill/optimization/qconv_v5/03_benchmark_qconv_v5.py --force
```

### 중단 시점에 확인된 것

- **링크 오류 해결됨.** 이전 빌드를 깨뜨린 `undefined reference to campp_qconv_v5_mac_3x3_sliding_raw`가
  사라졌다. 09:02~09:04에 `campp_operator_microbench` / `campp_operator_hotspot`이 정상 생성됐다.
  v5 실제 MAC 구현과 address 계층이 컴파일·링크를 통과한다.
- 남아 있는 09:02~09:07 바이너리 7개는 **새 v5 소스 기반**이다. 급하면 재빌드 없이
  `03_benchmark_qconv_v5.py`만 돌려 성능만 먼저 볼 수 있다. 단 `test_qconv_v5_primitives`·
  `test_qconv_v5_address`가 없어 커널 단위 경계 케이스(패딩·tail·부분 채널) 검증은 빠진다.

### v5 합격 조건

staging이 아니라 실제 MAC 경로이므로 **bitwise 동일은 필수, latency는 v4 대비 개선**이어야 한다.
NEON register pressure가 관건이므로 컴파일 경고와 spill 지표도 함께 볼 것.

---

## 3. 오늘 확정된 결과

### 3.1 E2E — v1 → v2

| | v1 | v2 |
|---|---:|---:|
| suite | `mac_fixed + combined_fixed + bn_combined + dequant_neon + remaining` | `v4 + combined_hybrid + bn_v2_spatial2 + dequant_neon + dqrq_neon + remaining` |
| CFLAGS | `-O3` | `-O3 -mcpu=cortex-a53 -flto` + aggressive |
| E2E mean | 522.322 ms | **345.741 ms** |
| p50 / p95 | 522.350 / 523.983 | 345.527 / 347.729 |
| **RTF** | 0.5223 | **0.3457** |

**분해** (플래그 혼입을 분리한 값):

| 축 | 배수 |
|---|---:|
| 커널 최적화 단독 | **1.4400×** |
| 컴파일러 플래그 단독 | 1.0491× |
| 총합 | **1.5107×** |

- retained-tensor bitwise: **PASS × 3 입력**
- 프로토콜: quick (warmup 5 / repeat 20 / baseline_repeat 5), overhead는 `preliminary`
- 결과: `results/profiling/e7_98/final_v2_aggressive/`
- 빌드: `build/profill/final_v2_aggressive/`

### 3.2 커널별 기여 (v1 → v2, ms/추론)

| family | ops | v1 | v2 | speedup | v2 share |
|---|---:|---:|---:|---:|---:|
| fused DQRQ | 57 | 105.503 | **2.630** | **40.12×** | 0.761% |
| BN | 55 | 39.904 | **11.734** | 3.40× | 3.394% |
| QConv | 115 | 205.952 | 168.498 | 1.22× | **48.735%** |
| fused QConv | 110 | 154.301 | 147.326 | 1.05× | **42.612%** |
| others | 379 | 11.053 | 8.676 | 1.27× | 2.509% |
| Dequant | 116 | 2.054 | 3.392 | **0.61× (퇴행)** | 0.981% |

감소분 176.6 ms 중 **fused DQRQ 하나가 102.9 ms(58%)**, BN v2가 28.2 ms(16%).

### 3.3 후보별 microbench (전부 bitwise 무결)

| 후보 | 결과 | 비고 |
|---|---|---|
| fused DQRQ `neon` | 35.83× (scalar 6.01× + NEON 5.96×) | 병목은 산술이 아니라 주소 계산이었음 |
| BN `v2_spatial2` | 3.42× | `v2_prescaled`(3.45×)와 접전, p95는 spatial2 우세 |
| fused QConv `combined_hybrid` | family 10.891× | `combined_fixed` 9.991×, `combined_v4` 9.803× |
| QConv `v4` | 3×3 28.23×, 1×1 32.37× | `mac_fixed` 대비 각 +34.9%, +5.1% |
| `mac_dispatch_guard` 제거 | 1.0118× (10 op 합) | guard 3×3 6.052→3.369%, 1×1 8.623→7.692% |

### 3.4 컴파일러 매트릭스 (quick, 6 후보 전부 통과)

| 후보 | p50 | RTF | binary |
|---|---:|---:|---:|
| `o2_generic` | 598.598 | 0.5986 | 155,864 |
| `o3_generic` | 527.028 | 0.5270 | 220,496 |
| `o3_a53` | 530.727 | 0.5307 | 220,496 |
| `o3_a53_lto` | 504.516 | 0.5045 | 210,880 |
| `compiler_default` | **501.612** | **0.5016** | 210,880 |
| `aggressive_packaged` ← 선택 | 502.370 | 0.5024 | **198,952** |

- `-mcpu=cortex-a53`는 **단독으로는 손해**(o3_generic 대비 −0.70%), LTO와 결합할 때만 이득
- stage 2 목표가 size라 더 느린 쪽이 선택됨. **최저 latency를 원하면 `compiler_default`**

---

## 4. 다음에 손댈 곳 (우선순위)

### 4.1 op 0 — 단일 최대 병목, 23.001%

`[32,1,3,3]`, 입력 채널 1개. **candidate 경로로 진입하지 못하고 production 커널로 직행**한다
(spill 프로파일링에서 확인: `campp_aarch64_qlinear_conv_o4i4`가 표본의 80%).
그래서 fixed·v4·hybrid 어느 것도 1.01×를 넘지 못한다. 79.5 ms로 v2 E2E의 23%.
**1채널 입력 전용 경로를 만들지 않으면 추가 개선의 상한이 된다.**

### 4.2 QConv 계열이 이제 E2E의 91.3%

`qlinear_conv_o4i4_v4` 48.735% + `fused_quant_..._combined_hybrid` 42.612%.
v5가 겨냥하는 지점이 정확히 여기다.

### 4.3 hotspot 분류기 미완 — `decision.ready=false`

미분류 표본이 3×3 **22%**, 1×1 **11%**. `foreign_symbol`은 0%이므로 외부 함수가 아니라
**v4/v5 심볼을 소스맵이 못 잡는 것**이다. 카테고리 이름이 전부 `v2_*` 계열로 나온다.
**raw perf 데이터가 이미 있으므로 재측정 없이 후처리만 고치면 된다.**
지금은 어디를 더 깎아야 하는지 판단 근거가 5분의 1만큼 비어 있다.

### 4.4 1×1의 남은 `mac_dispatch_guard` 7.692%

술어가 아니라 **경로 판정 분기**로 보인다(op 810은 8.661→8.645%로 거의 무변화).
hybrid dispatch가 shape별 경로를 정적으로 알고 있으므로, output block 진입 시점에
특수화된 타일 루프를 선택하면 타일당 분기를 없앨 수 있다.

### 4.5 Dequant 퇴행 원인 규명

v1 2.054 → v2 3.392 ms (0.61×). suite에서 `dequant_neon_combined`로 동일한데 느려졌다.
LTO 인라이닝 또는 캐시 배치 변화로 추정되나 **미확인**. 절대값 1.3 ms라 우선순위는 낮다.

---

## 5. 확정 측정으로 남은 것

- **official 프로토콜 E2E** (warmup 20 / repeat 100). 추론이 0.35초라 720회여도 **약 4.5분**.
  ```bash
  RESULT_TAG=final_v2_aggressive_official bash scripts/4_profill/03_profile_final_e7.sh --mode official --force
  ```
- **커널 축 1.44×는 환산값**이다. v1+aggressive를 497.884 ms로 추정해 계산했다
  (benchmark harness의 플래그 비율 4.68%를 profiler harness에 적용). 엄밀히 하려면
  v1 suite를 aggressive 플래그로 다시 빌드해 profiler harness로 측정해야 한다(약 5분).
- **BN v2 전 bucket 검증** (98/298/498/998). 특히 `v2_prescaled`의 동등성.
- **hybrid dispatch는 bucket 98 기준 6개 shape 화이트리스트**다. 다른 bucket에서 shape 구성이
  달라지면 미등록 shape는 전부 Fixed로 간다(안전하지만 놓칠 수 있음).

---

## 6. 작업 환경 이슈 (반복 발생)

### 6.1 빌드 스크립트에 타깃 필터가 없음

`01_build_optimization.sh`가 12개 타깃을 **무조건 전부** 컴파일한다. 보통 3개만 필요한데
매번 10분이 든다. `BUILD_SCOPE` 같은 옵션을 추가하면 반복 작업이 회당 7분씩 짧아진다.

### 6.2 git 분기가 반복됨

원인은 **`runs/`의 raw JSON을 커밋**하는 것. 한 번에 27만 줄까지 들어온다.
보드와 PC 양쪽에서 각각 결과를 만들면 매번 분기한다.
`pull.rebase=true`는 이 저장소에 설정해 뒀다. `git pull && git push` 순서를 지킬 것.
**`runs/**/raw/`를 `.gitignore`에 넣는 것을 권장**한다(결과 요약 `results/**`는 유지).

### 6.3 작업 트리 파일이 0바이트로 잘린 사고

커밋 `cb76f21` 이후 추적 파일 75개가 0바이트가 됐다. `git restore .`로 복구했고 유실은 없었다.
**빌드 전에 `git status`의 삽입/삭제 비대칭을 확인하는 습관**이 도움이 된다
(삽입 0줄 / 삭제 9,504줄이면 정상 편집이 아니다).

### 6.4 스크립트 시간 추정치를 믿지 말 것

여러 스크립트가 `profile_mean_ms`를 **최적화 이전** 프로파일에서 읽어 시간을 추정한다.
실제와 최대 9배까지 어긋난다(예: 45분 추정 → 실측 37초). perf 계열은 반대로
회당 오버헤드를 누락해 과소평가한다.

---

## 7. 주요 산출물 경로

```
results/profiling/e7_98/
├── final_combined/                        # v1 (RTF 0.5223)
├── final_v2_aggressive/                   # v2 (RTF 0.3457)  ← 현재 최고
├── compiler_matrix/compile_quick_01/      # 컴파일러 매트릭스
└── optimization/
    ├── fused_dequant_relu_quant/          # DQRQ 35.83×
    ├── bn_v2/                             # BN v2 3.42×
    ├── fused_qconv_family/                # hybrid 10.891×
    ├── qconv_v4/                          # v4 vs mac_fixed
    ├── qconv_hotspot_v4_10/               # guard 제거 전
    ├── qconv_hotspot_v4_10_noguard/       # guard 제거 후
    └── fused_qconv_spill_smoke/           # op 0 fallback 근거

build/profill/final_v2_aggressive/         # v2 바이너리 (metadata에 suite·flags 기록)
```

---

## 8. git 상태 (퇴근 시점)

- HEAD `8c8a41a` "비상", 원격과 **완전 동기 (0/0)**, 작업 트리 clean
- final suite는 **아직 v4**다. v5가 게이트를 통과한 뒤 `final_candidate_suite.c`에서 교체하는 순서.
